"""Milestone 6: leakage-safe statistical robustness, strategy selection,
and paper-trading promotion-gate framework. See `robustness.models`'s
module docstring for what this package does and does not do.

One-way dependency direction: this package may import from
`backtesting`, `calibration`, `execution`, `optimization`, `historical`,
`features`, `ml`, and `core`. None of those packages may import from
`robustness` -- enforced by convention and checked in this milestone's
own delivery report, not by a runtime import guard."""

from __future__ import annotations
